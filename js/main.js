
import { generateCommentsArray } from './data.js';

// Генерация массива из 25 комментариев для каждой фотографии
const commentsArray = generateCommentsArray();

// Для проверки результата можно вывести массив в консоль
console.log(commentsArray);
